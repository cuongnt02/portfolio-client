interface ProjectItemProps {
	title: string
	version: string
	buildStatus: string
	link: string
	percent: string
}

export function ProjectItem({ title, version, buildStatus, link, percent }: ProjectItemProps) {
	return (
		<div className="nes-container is-rounded">
		</div>
	)

}
